# habr-corpus-parser

Python 3.12, зависимости через pip + venv. Перед установкой новой зависимости — спроси.

К habr.com обращаемся аккуратно: задержка 2–3 секунды между запросами, внятный User-Agent.

Результаты парсинга — в corpus/, по одному markdown-файлу на статью.
